# Traefik 静态配置（生产：~/traefik/traefik.yml）
# 动态路由见 dynamic/routes.yml

api:
  dashboard: true
  insecure: true

entryPoints:
  web:
    address: ":8000"

providers:
  file:
    directory: /etc/traefik/dynamic
    watch: true

log:
  level: INFO
